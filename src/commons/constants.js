// token key is a RSA 256 cert encoded in base64
// const buff = Buffer.from(process.env.TOKENS_INTERNAL_SECRET, "base64");
// const tokenKey = buff.toString("utf-8");

const Constants = {
  // Headers and tokens
};

module.exports = Constants;
